# 開發 / 測試環境額外依賴。安裝方式：
#   pip install -r requirements.txt -r requirements-dev.txt
#
# 注意：fakeredis / lupa 僅供測試模擬 Redis；生產環境請用真實 Redis。

pytest==9.0.2
pytest-asyncio==1.3.0
pytest-mock==3.15.1
anyio==4.12.1
fakeredis==2.35.1
lupa==2.8
